// Loaded asynchronously by MotionProvider so animation features stay out of the
// initial bundle. `domMax` includes layout animations and drag.
import { domMax } from "motion/react";

export default domMax;
